# Fix: Ensure Drupal image style derivatives are generated for .webp files.
# The general media location block matches .webp before the styles/ block,
# preventing Drupal from generating derivatives on-demand.
location ~ ^/sites/.*/files/styles/.*\.webp$ {
    try_files $uri @rewrite;
    expires max;
    log_not_found off;
}
